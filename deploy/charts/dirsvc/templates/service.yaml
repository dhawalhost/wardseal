apiVersion: v1
kind: Service
metadata:
  name: {{ include "dirsvc.fullname" . }}
  labels:
    {{- include "dirsvc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dirsvc.selectorLabels" . | nindent 4 }}
